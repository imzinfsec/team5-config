apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: petcarelog-servicemonitor
spec:
  selector:
    matchLabels:
      app: petcarelog
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s